This project loads icons live from the Font Awesome CDN (see the <link> tag
in the <head> of every page), so no local icon files are required. If you
ever need a custom icon that Font Awesome doesn't have, drop an SVG here and
reference it directly, e.g. <img src="assets/icons/custom-icon.svg" alt="">.
